<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.abc.pagesql.dao.PageSqlDao">
    <select id="selectPublicItemList" parameterType="string" resultType="java.util.Map">
        ${sqlStr}
    </select>
    <insert id="insertPublicItemList" parameterType="string">
        ${sqlStr}
    </insert>
    <delete id="deletePublicItemList" parameterType="string">
        ${sqlStr}
    </delete>
    <update id="updatePublicItemList" parameterType="string">
        ${sqlStr}
    </update>
</mapper>